# ADR 0004: Risu 카드 런타임 전체 이식 — "번역"에서 "격리 실행 + 엔진 소유"로

- 상태: 승인
- 결정일: 2026-07-14
- 적용 범위: 카드 표현·변수·스크립트 계층, 보안 경계, 카나리아 전략
- 상위 결정: ADR 0002를 **개정** (제품 정체성·엔진 소유 원칙은 유지, 호환 계층 전략만 교체)

## 맥락

ADR 0002의 B안은 "리스의 Lua/CBS/정규식을 실행하지 않고 의미를 우리 엔진으로 번역한다"였다.
전제는 *시뮬 로직만 번역하면 표현은 안전한 부분집합으로 충분하다*는 것이었다.

실측이 그 전제를 무너뜨렸다. 부분집합 이식의 실패가 반복됐고(맨 이름 이미지 소각, 감정 변형 미해석,
`#when`/`module_enabled` 양쪽 분기 유출), 결정적으로 용사여관의 감정 스프라이트 소멸 사례에서
**표현·변수·스크립트가 분리 불가능한 한 덩어리**임이 증명됐다: 카드의 정규식이 만드는 에셋 이름은
카드의 Lua 트리거가 채우는 CBS 변수(`npc_*_outfit`)로만 완성된다. 정규식·CBS·에셋 해석을 전부
고쳐도 Lua가 돌지 않으면 그 셋을 잇는 변수가 비어 파이프라인이 끊긴다.

부분집합 전략의 유지비는 "카드 생태계 전체가 테스트 케이스"인 두더지잡기이며, 이미 전체 이식
비용을 넘어섰다.

## 결정

1. **RisuAI의 카드 대면 런타임을 전체 이식한다** (기준 커밋 `eb7780b`, 로컬 클론 `C:\risu`,
   GPL-3.0 → 우리 GPL-3.0-or-later, 호환):
   - CBS 파서(`cbs.ts`), 표시 파서(`parser.svelte.ts`), 정규식(`scripts.ts`),
     트리거 v1/v2(`triggers.ts`), **Lua 런타임(`scriptings.ts`, wasmoon)**
   - 리스의 DB·Svelte 스토어 결합부는 **어댑터 계약**으로 갈아끼운다.
     이식 파일은 업스트림 경로를 미러링해(`packages/risu/src/port/*`) diff 추적을 가능하게 한다.
2. **엔진은 계속 우리 것이다.** 세이브·롤백·세션 저널·근거 기억·무결성은 럭키 엔진이 소유하고,
   컴파일된 카드에서는 결정론 사실(골드·시간·전투)도 엔진이 소유한다.
3. **"임의 실행 금지"의 정신은 계승한다.** Lua는 wasmoon(WASM 샌드박스) 안에서, 리스가
   `declareAPI`로 등록한 유한 API(실측 54개)의 **우리 화이트리스트 부분집합**만으로 실행한다:
   - **역량별 기본 차단**: 네트워크(`request`), LLM/API 키(`LLMMain`·`axLLMMain`·`simpleLLM`),
     저장소·파일, DOM/window, 이미지 생성(`generateImage`), 엔진 이벤트 직접 발행,
     타 카드·타 채팅 접근 — 전부 차단이 기본값이다.
   - **1차 Lua 릴리스는 변수 계산 전용.** 위 역량은 각각 별도 결정으로만 열리며,
     비용이 발생하는 축(LLM/BYOK)은 열더라도 사용자 승인이 매 호출의 전제다.
   - 이는 DESIGN.md의 기존 조항("코드 실행을 도입한다면 capability로 분리하고 기본 거부")의 이행이다.
   - **단, 아래 「안전 게이트」가 전부 끝나기 전에는 Lua를 아예 실행하지 않는다.**

## 이중 상태 정책 (이 결정의 유일한 신규 설계 난제)

카드 Lua는 chatVar(카드 변수)로 자기 상태를 가질 수 있다. 엔진 상태와의 충돌은 3모드로 가른다:

| 모드 | 상태 소유 | 엔진 역할 |
|---|---|---|
| 일반/호환 (기본) | 카드 Lua(chatVar) | 세이브·롤백·저널·기억 — 심판 없이 기록관 |
| 완전 시뮬 (컴파일 후) | 엔진 | 카드 Lua의 상태 변경 API를 읽기 전용으로 강등, 표시만 허용 |

핵심 이점: 세션 스냅샷이 이미 `cbsVariables`를 무결성 해시까지 포함해 저장하므로,
**카드 Lua의 상태까지 우리 체크포인트가 되감는다.** 카드 무수정으로 리스가 구조적으로 못 주는
롤백·저널·근거 기억을 제공한다. 제품 문구가 단순해진다: *"완벽 호환이 기본, 엔진 컴파일은 업그레이드."*

## 마일스톤과 새 북극성 시험

M-A `scripts.ts`(정규식) → M-B `cbs.ts` → M-C 트리거 v1/v2 → **M-S0~S4 안전 게이트 + M-E 모드 정책** → M-D **Lua**.
M-D의 합격선이 새 카나리아다: **용사여관을 컴파일 없이 열어 스프라이트·상태창·태그가 그냥 작동한다.**
통과 시 DOMINIUM·Tesselia 카나리아는 이 런타임의 부분집합이므로 사실상 함께 통과한다.

## 안전 게이트 — Lua 실행과 트리거 프로덕션 배선의 **필수 선행 조건** (구속력 있음)

전체 이식은 카드 저작자에게 실행 권한을 넘긴다는 뜻이다. 그래서 아래 게이트가 **전부** 끝나기 전에는
**카드 Lua/JS를 실행하지 않으며, 트리거를 앱·세션에 배선하지 않는다.** `trigger-runtime.ts`는 그때까지
라이브러리로만 존재한다(테스트에서만 호출). 이 문단은 성능·일정 사유로 완화하지 않는다.

- **M-S0 표시 경로 봉인 (완료)** — 렌더는 영수증이지 거래가 아니다.
  `parseCbs`는 **기본 읽기 전용**(스크래치 복사본 + 업스트림 `runVar:false` 이중 방어)이며 `mutable:true`를
  명시한 트리거 트랜잭션에서만 쓴다. 채팅 본문·번역문·화자 추출·배경 HTML 어느 경로로 몇 번을 다시 그려도
  세션 변수는 변하지 않는다(`packages/risu/test/render-readonly.test.ts`가 이를 고정한다).
  업스트림도 표시 단계에 `runVar`를 넘기지 않으므로 **카드 호환성 손실은 없다.**
  업스트림 `applyMarkdownToNode`(innerHTML로 DOM 직접 생성)는 **영구 봉인**한다 — 호출 시 throw.
  **표시 경로는 영구 읽기 전용이고, 최종단 `sanitizeHtml`은 우회 불가능하다.** 두 성질은 이후 어떤
  마일스톤에서도 되돌리지 않는다.
- **M-S1 세션별 실행 격리** — 세션마다 독립 Worker에서 카드 런타임을 돌린다. 카드 전환 시 이전 Worker를
  종료한다. 전역 싱글턴 환경(`setCbsPortEnv` 등)이 세션 간에 새지 않는다.
  **Worker가 주는 것은 DOM 격리와 강제 종료 경계뿐이다.** Dedicated Worker 안에도 `fetch`·`WebSocket`·
  `IndexedDB`·`Cache`·`crypto`는 살아 있다. 따라서 **네트워크·저장소 차단은 Worker가 아니라 capability
  화이트리스트**(카드 코드에 그런 객체를 애초에 노출하지 않는 것)가 담당한다. Worker 번들 자체의 외부
  통신 경로도 정적 감사 대상이며, CSP는 보조 방어일 뿐 유일한 방어로 쓰지 않는다.
  Worker는 세션 상태를 직접 바꾸지 않는다: `상태 스냅샷 + revision`을 받아 `변수 patch + 효과 목록 + 경고`를
  반환하고, **메인 세션이 revision·권한을 검증한 뒤 원자적으로 저널에 기록**한다. 시간 초과 시 부분 변경은
  전부 폐기하고, 카드 전환 뒤 늦게 도착한 응답은 요청 ID 불일치로 버린다. Worker에는 이미지 바이너리가 아니라
  이름·메타데이터만 보낸다.
- **M-S2 실행 예산** — **시간 상한만으로는 메인 스레드를 지킬 수 없다.** 이미 시작된 JS는 밖에서 끊을 수
  없으므로 `Date.now()` 체크는 연산 사이에서만 듣고, 단 한 번의 무거운 연산(`{{#each {{range::5000000}}}}`)은
  중간에 못 끊는다. 그래서 **주 기준은 명령 수·항목 수·크기·깊이라는 구조적 상한**이고 시간은 보조 계측이다.
  - **M-S2a 표시 경로(메인 스레드, 완료)** — `security/budget.ts`가 입력·출력 문자 수, CBS 명령 실행 횟수,
    배열 생성 원소 수(`range` 등), `#each` 항목 수, 블록 중첩 깊이에 상한을 건다. 초과하면 파싱을 접고
    안전한 원문으로 되돌리며, 초과 사실은 진단에 남긴다(조용한 무시 금지). 상한은 실물 카드 9종 실측으로
    보정했다(최대 명령 수 2,222 / 최대 35ms / 하드·소프트 초과 0건 — 상한 대비 9배 여유).
  - **M-S2b 실행 경로(Worker, 완료)** — 선언형 트리거는 효과 실행 50,000회, 입력·출력 각 200만 자,
    트리거 2,000개, 효과·patch 각 20,000개를 상한으로 둔다. Worker는 소프트 250ms 경고 / 하드 1초
    강제 종료를 건다. 메모리는 Worker 종료에만 기대지 않고 입출력 크기로 선제 차단한다. 무한 루프는
    실제 Worker 브라우저 시험에서 UI가 계속 반응하는 동안 하드 상한에 종료됨을 확인했다. 저사양 모바일
    실측은 실제 배선 승인 뒤 보정하되, 정상 카드가 걸리면 상한을 올리기 전에 **어느 효과가 비싼지 먼저 기록**한다.
- **M-S3 효과 처분표** — 트리거 효과별로 적용/차단/승인 필요를 표로 못박는다. 조용한 무시는 금지 —
  차단된 효과는 반드시 기록에 남는다.
- **M-S4 결정론** — `Math.random`·시간에 세션 RNG와 논리 시각을 주입한다. 카드 변수 변경은 이벤트로
  저널링해 롤백·리플레이가 카드 상태까지 덮게 한다.
- **M-E 상태 소유 정책** — 호환 모드(카드 변수가 상태를 소유) / 완전 시뮬 모드(엔진이 소유, 카드 쓰기는
  읽기 전용 투영). Lua는 이 정책 없이는 켜지 않는다.
- **M-D Lua는 마지막** — wasmoon, **역량별 기본 차단**(네트워크·LLM/API 키·저장소·파일·DOM·이미지 생성·
  엔진 이벤트·타 카드/채팅 접근 전부 불가). 1차 릴리스는 **변수 계산 전용**.

## 유지되는 것 / 감수하는 것

- 유지: 결정론 커널·모듈·컴파일러·조종석(HUD·결정 카드·로스터)·보안 척추 전부.
  `ysp-translate` 등 태그 번역기는 완전 시뮬 모드 전용으로 재배치(폐기 아님).
- 감수: 업스트림 추적 부담(커밋 고정 + 주기 diff), 어댑터 접합 공수(DB 결합 실측 108곳),
  번들 +271KB(wasmoon WASM — 기존 SQLite WASM 865KB보다 작음), Lua 실행 성능 측정 필요.
- 우리 자체 강화(정규식 시간·길이 예산, catastrophic 패턴 스킵, 최종단 sanitize)는
  이식 코드 **바깥의 래퍼**로 유지한다 — 업스트림 의미론을 오염시키지 않으면서 방어를 지킨다.
