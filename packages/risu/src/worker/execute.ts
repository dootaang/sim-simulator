import{runCardTriggers}from'../trigger-runtime.ts';import type{RuntimeWorkerRequest,RuntimeWorkerResponse,VariablePatch}from'./contract.ts';
const diff=(before:Record<string,string>,after:Record<string,string>):VariablePatch[]=>{const out:VariablePatch[]=[];for(const[key,value]of Object.entries(after))if(before[key]!==value)out.push({op:'set',key,value});for(const key of Object.keys(before))if(!Object.hasOwn(after,key))out.push({op:'delete',key});return out;};
export async function executeRuntimeRequest(request:RuntimeWorkerRequest):Promise<RuntimeWorkerResponse>{const started=Date.now(),warnings:RuntimeWorkerResponse['warnings']=[];try{const result=await runCardTriggers({...request.input,variables:{...request.snapshot.variables},alert:(kind,message)=>warnings.push({code:kind==='error'?'trigger_error':'trigger_notice',message})});return{type:'result',ok:true,requestId:request.requestId,sessionId:request.snapshot.sessionId,cardId:request.snapshot.cardId,baseRevision:request.snapshot.revision,patch:diff(request.snapshot.variables,result.variables),effects:[],warnings,result:{displayData:result.displayData,stopSending:result.stopSending,ephemeral:result.ephemeral},durationMs:Date.now()-started};}catch(error){return{type:'result',ok:false,requestId:request.requestId,sessionId:request.snapshot.sessionId,cardId:request.snapshot.cardId,baseRevision:request.snapshot.revision,error:error instanceof Error?error.message:String(error),warnings,durationMs:Date.now()-started};}}
