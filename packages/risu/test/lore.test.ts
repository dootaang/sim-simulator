import{describe,expect,it}from'vitest';import{activateLore,applyRegexPipeline,normalizeLoreEntries,resolveModules}from'../src/index.ts';
describe('safe Risu content compatibility',()=>{it('activates constant and selective lore in deterministic order',()=>{const entries=normalizeLoreEntries([{id:'b',content:'B',key:'inn',order:2},{id:'a',content:'A',constant:true,order:1},{id:'c',content:'C',key:'silvia',secondkey:'wage',selective:true,order:3}]);expect(activateLore(entries,[{content:'silvia discusses wage at the inn'}]).entries.map((value)=>value.id)).toEqual(['a','b','c']);});it('runs safe regex stages and rejects nested quantifiers',()=>{const result=applyRegexPipeline('hello foo',[{id:'ok',stage:'editoutput',pattern:'foo',replacement:'bar',flags:'g',enabled:true},{id:'bad',stage:'editoutput',pattern:'(a+)+',replacement:'x',flags:'g',enabled:true}],'editoutput');expect(result).toEqual({text:'hello bar',warnings:[{id:'bad',reason:'unsafe_pattern'}]});});it('uses later module scopes and blocks executable capabilities',()=>{const result=resolveModules([[{id:'a',namespace:'n',scope:'global',capabilities:['lore'],enabled:true}],[{id:'b',namespace:'n',scope:'character',capabilities:['regex'],enabled:true},{id:'x',namespace:'x',scope:'chat',capabilities:['lua'],enabled:true}]]);expect(result.modules.map((value)=>value.id)).toEqual(['b']);expect(result.blocked.map((value)=>value.id)).toEqual(['x']);});});
