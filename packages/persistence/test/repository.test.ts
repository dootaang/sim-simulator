import{describe,expect,it}from'vitest';import{createMemoryRepository}from'../src/index.ts';describe('repository',()=>{it('clones, orders and filters sessions',async()=>{const repo=createMemoryRepository<{turn:number}>();await repo.put({id:'a',schemaHash:'x',title:'A',updatedAt:1,payload:{turn:1}});await repo.put({id:'b',schemaHash:'x',title:'B',updatedAt:2,payload:{turn:2}});const value=await repo.get('a');if(value)value.payload.turn=99;expect((await repo.get('a'))?.payload.turn).toBe(1);expect((await repo.getLatest('x'))?.id).toBe('b');expect((await repo.list()).map((row)=>row.id)).toEqual(['b','a']);});});
