import{describe,expect,it,vi}from'vitest';import{browserStorageStatus}from'../src/storage-health.ts';
describe('browser storage health',()=>{it('requests persistence and reports capacity without promising success',async()=>{const persist=vi.fn(async()=>false),status=await browserStorageStatus(true,{persisted:async()=>false,persist,estimate:async()=>({usage:25,quota:100})});expect(persist).toHaveBeenCalledOnce();expect(status).toEqual({supported:true,persistent:false,requested:true,usage:25,quota:100,error:null});});it('does not request again after permission is granted',async()=>{const persist=vi.fn(async()=>true),status=await browserStorageStatus(true,{persisted:async()=>true,persist,estimate:async()=>({})});expect(persist).not.toHaveBeenCalled();expect(status.persistent).toBe(true);});it('reports unsupported environments honestly',async()=>expect(await browserStorageStatus(true,undefined)).toEqual({supported:false,persistent:null,requested:false,usage:null,quota:null,error:null}));});
