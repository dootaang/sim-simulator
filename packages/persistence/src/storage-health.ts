export interface StorageManagerLike{persisted?:()=>Promise<boolean>;persist?:()=>Promise<boolean>;estimate?:()=>Promise<{usage?:number;quota?:number}>}export interface BrowserStorageStatus{supported:boolean;persistent:boolean|null;requested:boolean;usage:number|null;quota:number|null;error:string|null;}
export async function browserStorageStatus(request=false,manager:StorageManagerLike|undefined=typeof navigator==='undefined'?undefined:navigator.storage):Promise<BrowserStorageStatus>{if(!manager)return{supported:false,persistent:null,requested:false,usage:null,quota:null,error:null};let persistent:boolean|null=null,requested=false,usage:number|null=null,quota:number|null=null,error:string|null=null;try{persistent=manager.persisted?await manager.persisted():null;if(request&&persistent===false&&manager.persist){persistent=await manager.persist();requested=true;}if(manager.estimate){const estimate=await manager.estimate();usage=typeof estimate.usage==='number'?estimate.usage:null;quota=typeof estimate.quota==='number'?estimate.quota:null;}}catch(reason){error=reason instanceof Error?reason.message:String(reason);}return{supported:!!manager.persisted||!!manager.persist,persistent,requested,usage,quota,error};}
