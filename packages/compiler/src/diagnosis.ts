import type{ParsedCard}from'@simbot/card';import type{CompilerCoverage}from'./compiler-prompt.ts';import type{MinedCard}from'./lua-mine.ts';
export function diagnoseCard(parsed:ParsedCard,mined:MinedCard,coverage:CompilerCoverage){const d=(parsed.card.data??parsed.card)as Record<string,unknown>,text=JSON.stringify(d),rules:Array<[string,RegExp,string]>=[['combat',/전투|combat|battle/i,'combat.turnbased'],['quests',/퀘스트|의뢰|mission|quest/i,'rpg.quests'],['economy',/가격|골드|상점|price|gold|shop/i,'core.inventory'],['facilities',/시설|기지|facility|base management/i,'core.facilities']],capabilities=rules.flatMap(([id,re,module])=>re.test(text)?[{id,module,evidence:['카드/로어']}]:[]),issues=[...mined.warnings.map(message=>({level:'warn',code:'embedded_runtime_code',message,evidence:[]})),...(coverage.omittedEntries?[{level:'warn',code:'compiler_input_trimmed',message:`로어 ${coverage.omittedEntries}개가 길이 상한으로 제외됐습니다.`,evidence:coverage.omitted.map(e=>e.name)}]:[])];return{contract:'card-diagnosis/0.1' as const,card:{name:parsed.name,format:parsed.format,spec:parsed.spec,source:parsed.source??''},classification:mined.luaSize?'script-assisted-sim':capabilities.length>=3?'prose-sim':'narrative-card',runtime:{embeddedModule:mined.moduleSummary,luaChars:mined.luaSize,defaultVariableLines:mined.defaultVars.totalLines},capabilities,suggestedModules:[...new Set(capabilities.map(c=>c.module))],compilerCoverage:coverage,issues};}
