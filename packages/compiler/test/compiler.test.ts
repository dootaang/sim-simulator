import{describe,expect,it}from'vitest';import type{ParsedCard}from'@simbot/card';import{compileCardDraft}from'../src/index.ts';
function card(body:Record<string,unknown>):ParsedCard{return{format:'json',source:'x.json',spec:'chara_card_v3',specVersion:'3',name:'Test',card:{spec:'chara_card_v3',data:{name:'Test',...body}},assets:[],containerEntries:[],sourceBytes:new TextEncoder().encode('{}')}}
describe('safe draft compiler',()=>{it('does not install a genre from prose evidence alone',()=>{const result=compileCardDraft(card({description:'헌터가 게이트를 공략하는 이야기'}));expect((result.project.manifest.modules as Record<string,unknown>).installed).toEqual([]);expect(result.report.candidates.find((value)=>value.id==='genre.hunter')?.status).toBe('suggested');});it('installs only approved genres and generates their screens',()=>{const result=compileCardDraft(card({description:'용사의 여관 주점 숙박 경영'}),['genre.inn']);expect((result.project.manifest.modules as Record<string,unknown>).installed).toEqual(['genre.inn']);expect(result.project.manifest.runtime.screens.map((value)=>value.id)).toEqual(['play','management']);});it('keeps unrelated cards genre neutral',()=>{const result=compileCardDraft(card({description:'school conversation'}));expect(result.report.candidates.every((value)=>value.status==='rejected')).toBe(true);expect(result.project.manifest.runtime.screens).toHaveLength(1);});});
